schema {
  query: Query
}

type Query {
  menu(id: Int!): Menu
}

type Menu implements NodeInterface {
  id: Int!
  title: String!
  groups: [Groups]
}

type Groups {
  name: String
}

interface NodeInterface {
  id: Int!
}
